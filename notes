Pour version 1.0
 * résoudre le bug du cr cr lf sous windows -> ok
 * minimiser le diff sur la config -> ok
 * aller à la ligne dans les infobulles -> ok
 * refaire le layout -> ok
 * mettre en place le workflow de publication de releases
 * bug écriture si spinbox vide
 * limiter fréquence d'écriture à cause des perfs du mass storage
 * afficher un message quand la conteuse n'est pas détectée
 * ajouter une zone de log en bas de la fenêtre
 * tester avec du vrai hw
 * tester sur windows :grin:

détection des conteuses présentes ? -> ok


Onglets :
  * tutoriels
  * bibliothèque
  * histoires / musiques
  * paramètres
  * bruitages

Fonctionnalités :
  * changement de la configuration
  * lecture des tutoriels vidéo
  * changement des musiques / histoires
  * changement des sons
  * mise à jour
  * internationalisation

# dependences de build
apt install software-properties-common

# add at least contrib section
add-apt-repository contrib
dpkg --add-architecture i386
apt update

apt install wine winetricks xvfb git make python3 python3-tk devscripts expect wget wine32
